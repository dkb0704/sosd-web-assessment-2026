<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fzujxl.aicomputerplatform.mapper.user.UserMapper">
    <update id="updateUser">
        UPDATE user
        <set>
            <if test='userName != null and userName != ""'>
                user_name = #{userName},
            </if>
            <if test='email != null and email != ""'>
                email = #{email},
            </if>
            <if test='password != null and password != ""'>
                password = #{password},
            </if> update_time = NOW()
            token_version = token_version_version + 1
        </set>
        WHERE id = #{id}
    </update>
    <select id="selectByAccount"
            resultType="com.fzujxl.aicomputerplatform.entity.User">
            (select id from 'user' where deleted = 0 and user_name = #{account} )
            UNION ALL
            (select id from 'user' where deleted = 0 and email = #{account} )
            Limit 1
    </select>
    <select id="selectVersion"
            resultType="com.fzujxl.aicomputerplatform.entity.User">
            (select token_version from 'user' where deleted = 0 and user_name = #{uniqueKey} )
            UNION ALL
            (select token_version from 'user' where deleted = 0 and email = #{uniqueKey} )
            Limit 1
    </select>
</mapper>