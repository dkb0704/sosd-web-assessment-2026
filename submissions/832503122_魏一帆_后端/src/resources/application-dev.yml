spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://dev-db:3306/task_management_app?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}