<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.computingpowerrental.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.computingpowerrental.entity.User">
        <id property="id" column="id"/>

        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="computePoints" column="compute_points"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT
        id, username, password, email, nickname, avatar, phone,
        role, status, compute_points, created_at, updated_at
        FROM user
        WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT
        id, username, password, email, nickname, avatar, phone,
        role, status, compute_points, created_at, updated_at
        FROM user
        WHERE username = #{username}
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT
        id, username, password, email, nickname, avatar, phone,
        role, status, compute_points, created_at, updated_at
        FROM user
        WHERE email = #{email}
    </select>

    <select id="findByAccount" resultMap="UserResultMap">
        SELECT
        id, username, password, email, nickname, avatar, phone,
        role, status, compute_points, created_at, updated_at
        FROM user
        WHERE username = #{account}
        OR email = #{account}
    </select>

    <insert id="insert" parameterType="com.example.computingpowerrental.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (
        username, password, email, nickname, avatar, phone,
        role, status, compute_points
        )
        VALUES (
        #{username}, #{password}, #{email}, #{nickname}, #{avatar}, #{phone},
        #{role}, #{status}, #{computePoints}
        )
    </insert>

    <update id="update" parameterType="com.example.computingpowerrental.entity.User">
        UPDATE user
        <set>
            <if test="nickname != null">
                nickname = #{nickname},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE user
        SET status = #{status},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateComputePoints">
        UPDATE user
        SET compute_points = #{computePoints},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="increaseComputePoints">
        UPDATE user
        SET compute_points = COALESCE(compute_points, 0) + #{points},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="decreaseComputePoints">
        UPDATE user
        SET compute_points = compute_points - #{points},
        updated_at = NOW()
        WHERE id = #{id}
        AND compute_points >= #{points}
    </update>

    <update id="reducePoints">
        UPDATE user
        SET compute_points = compute_points - #{points}
        WHERE id = #{userId}
        AND compute_points >= #{points}
    </update>

    <!-- 管理端分页查询用户 -->
    <select id="findPage"
            resultMap="UserResultMap">

        SELECT
        id, username, password, email, nickname, avatar, phone,
        role, status, compute_points, created_at, updated_at
        FROM user

        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>

        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}

    </select>


    <!-- 管理端查询用户总数 -->
    <select id="countUsers"
            resultType="java.lang.Long">

        SELECT COUNT(*)
        FROM user

        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>

    </select>

</mapper>